name: PX4 Firmware Build

on:
  pull_request:
    paths:
      - 'src/**'
      - 'boards/**'
      - 'cmake/**'
      - 'CMakeLists.txt'
      - 'Kconfig'
  workflow_dispatch:

jobs:
  build-nuttx:
    runs-on: ubuntu-latest
    container:
      image: px4io/px4-dev-nuttx-focal:latest
    strategy:
      fail-fast: false
      matrix:
        target:
          - px4_fmu-v5_default
          # Add more targets as needed:
          # - px4_fmu-v6x_default
          # - custom_board_default
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Build ${{ matrix.target }}
        run: make ${{ matrix.target }}

      - name: Upload firmware artifact
        uses: actions/upload-artifact@v4
        with:
          name: firmware-${{ matrix.target }}
          path: build/${{ matrix.target }}/*.px4

  build-linux:
    runs-on: ubuntu-latest
    # Uncomment and configure for T113/RISC-V builds
    # container:
    #   image: your-riscv-toolchain-image:latest
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Install RISC-V toolchain
        run: |
          echo "TODO: Configure RISC-V cross-compilation toolchain"
          echo "This job is a placeholder - configure for your T113 target"
          # sudo apt-get install -y gcc-riscv64-linux-gnu
          # make BOARD=t113 CROSS_COMPILE=riscv64-linux-gnu-

      # - name: Build for T113
      #   run: make t113_default
